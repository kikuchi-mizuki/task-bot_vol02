{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -lc 'set -xe; echo [BOOT] starting; python -V; which python; env | sed -n 1,50p; python -u app.py || (echo [BOOT] python exited:$?; sleep 600)'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "environments": {
    "production": {
      "variables": {
        "TZ": "Asia/Tokyo"
      }
    }
  }
}
